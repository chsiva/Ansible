---

- hosts: localhost
  tasks:
  - name: Install httpd service
    command: whoami
    register: out
    
  - debug:
      var: out
#     yum:
#       name: httpd
#       state: latest
#   - name: Restart httpd
#     service: name=httpd state=started  

